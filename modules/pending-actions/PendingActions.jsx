import Section from '../../shared/Section'

export default function PendingActions() {
  return (
    <Section
      title="Pending actions"
      note="To-do for this company — declarations to generate, validate, or send"
      height={200}
    />
  )
}
